# Default Kibana configuration for docker target
server.host: "0.0.0.0"
server.shutdownTimeout: "5s"

elasticsearch.hosts: ["https://elasticsearch:9200"]
elasticsearch.username: "kibana_system"
elasticsearch.password: ""

xpack.encryptedSavedObjects.encryptionKey: "<ADD_HERE (32_characters)>"

elasticsearch.ssl.verificationMode: none

server.ssl.enabled: true
server.ssl.key: /usr/share/kibana/config/kibana_node_key.pem
server.ssl.certificate: /usr/share/kibana/config/kibana_node.pem

monitoring.ui.container.elasticsearch.enabled: true
xpack.task_manager.monitored_stats_health_verbose_log.enabled: true
xpack.reporting.roles.enabled: false
xpack.reporting.kibanaServer.hostname: localhost
